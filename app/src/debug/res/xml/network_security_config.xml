<?xml version="1.0" encoding="utf-8"?>
<!--
  Só no debug: o Firebase Local Emulator Suite fala HTTP sem TLS. Libera tráfego
  sem criptografia apenas para o host do computador visto pelo emulador Android
  (10.0.2.2) e para localhost; o resto continua exigindo HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
