# Memória do daemon do Gradle.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
# Pula a fase de configuração quando os scripts de build não mudaram.
org.gradle.configuration-cache=true

# Versão do app — incremente ovigia.versionCode a cada publicação na loja.
ovigia.versionCode=2
ovigia.versionName=1.1.0
